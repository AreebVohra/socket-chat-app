export const BaseURL = 'http://192.168.0.34:3000';

export const Endpoints = {
    login: `${BaseURL}/api/login`,
    register: `${BaseURL}/api/register`,
    userImage: `${BaseURL}/uploads/avatars/`
}

// export default Endpoints;